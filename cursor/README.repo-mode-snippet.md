<!-- Copy the section below into the target repository README (edit the tag value). -->

## Repo mode (Cursor workstation)

- **Primary mode tag:** `analysis`
- **Machine-readable marker:** see root file `REPO_MODE` (single line, same value as above).
- **Cursor rules:** use `.cursor/rules/rule-analysis.mdc` from this station template.
- **Skill bundles:** when you need methodology depth, attach the relevant `SKILL.md` from this repo’s `Skills/` tree.
